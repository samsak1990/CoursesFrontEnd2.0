Глубокое сравнение объектов
📌 Задача: Напиши функцию deepEqual(a, b), которая сравнивает два объекта с учетом вложенных структур.

Реализация Promise.all с нуля
📌 Задача: Реализуй myPromiseAll, аналог Promise.all, который принимает массив промисов и возвращает промис, резолвящийся массивом результатов.

Реализация EventEmitter
📝Описание
Нужно реализовать класс EventEmitter, который позволяет работать с событиями. Этот класс должен поддерживать:
Подписку на события (on) – позволяет добавить обработчик для конкретного события.
Одноразовые подписки (once) – обработчик сработает только один раз, после чего удалится.
Отписку (off) – удаляет обработчик события.
Вызов события (emit) – вызывает все подписанные обработчики события.
Пример работы
const emitter = new EventEmitter();

function onMessage(data) {
console.log("Получено сообщение:", data);
}

emitter.on("message", onMessage);
emitter.emit("message", "Hello!"); // Выведет: Получено сообщение: Hello!

emitter.off("message", onMessage);
emitter.emit("message", "Hello again!"); // Ничего не выведет

emitter.once("greet", (name) => {
console.log(`Привет, ${name}!`);
});

emitter.emit("greet", "Андрей"); // Выведет: Привет, Андрей!
emitter.emit("greet", "Андрей"); // Ничего не выведет, т.к. обработчик удалён после первого вызова
